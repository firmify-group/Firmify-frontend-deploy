export enum PATH_ROUTES {
    HOME = '/',
    LOGIN = '/login',
    MANAGER_HOME = '/manager/home',
    MANAGER_REQUEST = '/manager/requests',
    MANAGER_USERS = '/manager/users',
    USER_PROCESS = '/client/process',
}

export enum ROLE {
    ADMIN = 'SUPERVISOR',
    USER = 'EMPLEADO',
}